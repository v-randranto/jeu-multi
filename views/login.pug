<!DOCTYPE html>
html(lang="fr")
    head
        meta(charset="UTF-8")
        title Accueil   
        link(rel="stylesheet", href="css/style.css")     
    body            
        - var pseudoValue = '';
        - if (pseudo) pseudoValue = pseudo;
        
        form(id="loginForm" action="/login", method="POST")
            p Pseudo : 
                input(type="text", name="pseudo" value=pseudoValue,    onfocus="this.value=''")
            p Mot de passe : 
                input(type="text", name="pwd", onfocus="thivalue=''")
            p 
                button Valide
            p(class="msg error")=msgError             
            p(class="msg info")=msgInfos 
        p 
            a(href="/register" class="connectLink") Comment, on ne se connait pascliquez vite ici !
